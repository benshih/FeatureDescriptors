% Benjamin Shih
% 10/18/2013
% 16720 HW3 BRIEF Feature Descriptions
% 2.4 Descriptor Matching

% Use the Hamming distance (percentage of bits in two descriptors that
% differ) as the distance metric to compute the similarity between two
% descriptors. 

function [matches] = briefMatch(desc1, desc2, ratio)
    [m, ~] = size(desc1);
    [n, nbits] = size(desc2);
    
    
end